{
    "title": "Input schema of Google Maps Lead Scraper",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "searchQuery": {
            "title": "Search Query",
            "type": "string",
            "description": "What you want to search on Google Maps, e.g., 'cafes in Mumbai'.",
            "default": "cafes in Mumbai",
            "editor": "textfield"
        },
        "maxResults": {
            "title": "Max Results",
            "type": "integer",
            "description": "Maximum number of places to scrape.",
            "default": 50
        }
    },
    "required": ["searchQuery"]
}
