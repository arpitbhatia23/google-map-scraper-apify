{
    "actorOutputSchemaVersion": 1,
    "title": "Google Maps Leads Output",
    "properties": {
        "name": {
            "type": "string",
            "title": "Business Name",
            "template": "{{name}}"
        },
        "address": {
            "type": "string",
            "title": "Address",
            "template": "{{address}}"
        },
        "phone": {
            "type": "string",
            "title": "Phone Number",
            "template": "{{phone}}"
        },
        "website": {
            "type": "string",
            "title": "Website",
            "template": "{{website}}"
        },
        "rating": {
            "type": "number",
            "title": "Rating",
            "template": "{{rating}}"
        },
        "reviews": {
            "type": "number",
            "title": "Number of Reviews",
            "template": "{{reviews}}"
        },
        "url": {
            "type": "string",
            "title": "Google Maps URL",
            "template": "{{url}}"
        }
    }
}
